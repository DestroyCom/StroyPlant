# Security Memory
> Auto-generated by security-skill v1.0.0
> Do not delete — used by the AI to track security state
> Last updated: 2026-08-17

## Project Profile
- detected_stacks: [Node.js/TypeScript, Fastify, Prisma/SQLite, tRPC, BetterAuth, MCP SDK (@modelcontextprotocol/sdk), Docker, GitHub Actions, React/Vite, pnpm workspace]
- deployment_platforms: [self-hosted Debian production server via Docker, GHCR image publish]
- databases: [SQLite via Prisma]
- has_ai_features: true  # MCP server (backend/src/mcp/) exposing 4 tools over OAuth 2.1
- compliance_mode: none  # single-admin personal tool, operator = sole data subject
- environment: prod  # self-hosted production server + local dev

## User Preferences
- ask_before_modify: true        # Always ask before modifying existing files
- ask_before_design_change: true # Always ask if visual/UI might be affected
- language: auto                 # auto | fr | en | es | de
- report_format: file            # file | chat
- rotation_reminder_days: 90     # Remind when secrets older than N days

## Security Score History
| Date | Score | Critical | High | Medium | Notes |
|------|-------|----------|------|--------|-------|
| 2026-08-17 | 84/100 🟡 | 0 | 2 | 3 | First full audit. High: no HTTP security headers, Docker container runs as root. Full detail in security-report.md |
| 2026-08-17 (fix pass) | 89/100 🟡 | 0 | 1 | 0 | helmet+CSP, rate-limit, tRPC stack strip, hono/fast-uri bump, Actions SHA-pinning, pre-commit hook, dependabot.yml all applied+verified same day. Only Docker-root-user (High) remains open — needs production BlueZ verification before dropping privileges. |

## Accepted Risks
# Risks you have reviewed and accepted. Add entries below.
# Format:
# - risk: description
#   reason: why you accept it
#   accepted_on: YYYY-MM-DD
accepted_risks:
  - risk: MQTT broker password stored in cleartext in the DB (MqttSettings)
    reason: consistent with BETTER_AUTH_SECRET's own handling, no vault for this single-admin deployment
    accepted_on: 2026-07-28
  - risk: MCP OAuth allowDynamicClientRegistration is open (no allowlist)
    reason: any registered client still must pass through the real /login page, gated by the single admin password
    accepted_on: 2026-07-28
  - risk: SyncEvent/RawSensorLog have no retention/pruning policy (unbounded growth)
    reason: prefer dedup/archive over deleting historical/log data; deferred until real production volume informs a policy
    accepted_on: 2026-07-31
  - risk: No GDPR data-export/erasure self-service endpoint
    reason: operator and sole data subject are the same person with direct DB access; self-service erasure would be theater
    accepted_on: 2026-08-17

## Never Ask Again
# Rule IDs to skip confirmation for (already reviewed)
never_ask_again: []

## Custom Rules
# Override default behavior. Examples:
# - IGNORE: CORS https://partner.com (known partner API)
# - IGNORE: PORT 5432 (internal DB, expected)
custom_rules: []

## Secrets Lifecycle
# Track when secrets were last rotated
# Format:
# SECRET_NAME:
#   created: YYYY-MM-DD
#   last_rotated: YYYY-MM-DD
#   rotation_policy: 90days
#   status: ok | due | overdue
secrets: {}

## Incident History
- none
