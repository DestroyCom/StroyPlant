-- CreateTable
CREATE TABLE "RawSensorLog" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "readingId" INTEGER NOT NULL,
    "lightRaw" INTEGER,
    "soilConductivityRaw" INTEGER,
    "soilTempRaw" INTEGER,
    "airTempRaw" INTEGER,
    "soilMoistureRaw" INTEGER,
    "soilMoistureCalibrated" REAL,
    "airTempCalibrated" REAL,
    "luminosityCalibrated" REAL,
    "eaRaw" REAL,
    "ecbRaw" REAL,
    "ecPorousRaw" REAL,
    "waterTankLevelPercent" INTEGER,
    "watVwcIrr" INTEGER,
    "watVwcCmd" INTEGER,
    "watNIrr" INTEGER,
    "watPumpDutyCycle" INTEGER,
    "watVwcIrrEco" INTEGER,
    "watVwcCmdEco" INTEGER,
    "watNIrrEco" INTEGER,
    "watMode" INTEGER,
    "watTimeSlotStart" INTEGER,
    "watTimeSlotDurr" INTEGER,
    "watVacationStart" INTEGER,
    "watVacationEnd" INTEGER,
    "algorithmStatus" INTEGER,
    "plantDrStatusFlagsRaw" INTEGER,
    "plantDrDryN" INTEGER,
    "plantDrDryVwcRaw" INTEGER,
    "plantDrWetN" INTEGER,
    "plantDrWetVwcRaw" INTEGER,
    "plantDrConfigId" INTEGER,
    "plantDrNextWateringDate" INTEGER,
    "plantDrNextEmptyTankDate" INTEGER,
    "plantDrFullTankAutonomy" INTEGER,
    "calibrationDataBlobHex" TEXT,
    "colorRaw" INTEGER,
    "tempRaw" INTEGER,
    "humidityRaw" INTEGER,
    "voltageRawMv" INTEGER,
    CONSTRAINT "RawSensorLog_readingId_fkey" FOREIGN KEY ("readingId") REFERENCES "Reading" ("id") ON DELETE RESTRICT ON UPDATE CASCADE
);

-- CreateIndex
CREATE UNIQUE INDEX "RawSensorLog_readingId_key" ON "RawSensorLog"("readingId");
