- [FIXED 2026-07-30, confirmed on real hardware] Missing soil fertility index, we have humidty, temperature, and luminosity, but no soil fertility index. This is important for understanding the overall health of the soil and its ability to support plant growth. And it was on WatchFlower and the original Parrot app, so it should be included in the project as well. Check thoses project, and see how they implemented it, and if we can use the same approach or improve upon it.
  - Investigated 2026-07-30: the two characteristics we were trying to read (39e1fa0d/0e) fail 100% of the time on both real Parrot Pots ("Characteristic not available"). Fetched WatchFlower's actual Parrot Pot driver source (device_parrotpot.cpp) — it reads a different raw characteristic (39e1fa02) with a known conversion formula. Switched to that (ble/parrot/soilConductivity.ts), replacing the old soilConductivityEcb/EcPorous fields with soilConductivityUsCm end to end (Prisma migration, Health Engine, frontend gauge/chart "Fertilité du sol").
  - Cross-checked against the official app's own decompilation (local parrot-pot-debug/analyse/decoded_jadx) before trusting WatchFlower: the official app never subscribes to fa02 for either device type, but that's silence, not a contrary signal. Also confirmed WatchFlower calibrates fa02 differently per device model (Flower Power: raw/1.771; Pot: clamp+inverted-map) — used the Pot-specific formula, correctly.
  - Empirically confirmed on real hardware (2026-07-30): briefly stopped the stroyplant container, ran a disposable one-off container against the real A0:14:3D:CD:A0:73 — fa02 responds (raw=757), alongside a working sanity-check read on fa09 (VMC, 30.96%). Container restarted immediately after.
  - Open follow-up (not blocking): raw=757 falls outside WatchFlower's assumed calibration range [1500,2036], clamping to the max of our 0-1000 scale. May need retuning the constants with more real readings over time (e.g. before/after a fertilizer event) — not guessed from this single data point.
- [FIXED 2026-07-30] A bug occured, where it seems that the custom devices names are been overided, and not displaying in the welcome page. This is a problem because it makes it difficult for users to identify their devices, especially if they have multiple devices connected. The custom names should be preserved and displayed correctly in the welcome page.
  - Root cause: discoverySession.ts's onDeviceSeen overwrote `name` on every re-scan of an already-claimed device. Fixed, verified against a scratch copy of dev.db.
- [FIXED 2026-07-30, not yet redeployed] In production, live mode not working, continously displaying "le-connection-abort-by-local" some minutes after the start.
  - Root cause (confirmed via real docker logs on the production server): subscribeLive's initial connectDevice() call had zero retry, unlike every other BLE operation. Added the same retry policy to the initial connection only.
- [FIXED 2026-07-30, not yet redeployed] In prodution, in the history page, a large niumber of errors are displayed, wth the message "Operation already in progress".
  - Root cause (confirmed via real docker logs, 308 occurrences/6h): a timed-out connect() was never cancelled on the BlueZ side, so the next retry collided with it. Fixed by disconnecting on a failed connect().
- [DONE 2026-07-30] For now, the cron to refresh the data is set to run every 5 minutes, but this may not be optimal for all users. We need to add in the settings page an option for users to customize the refresh interval according to their needs. This will allow users to balance between data freshness and resource usage, especially for those with limited bandwidth or battery life.
  - Added a "Synchronisation" card in /settings (poll-settings-section.tsx), backed by a new DB-backed PollSettings row (replaces the old PARROT_POLL_INTERVAL_MS env var), applied live with no restart.
